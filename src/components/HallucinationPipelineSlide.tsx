/**
 * Modulio 7 sk. 67.7 – tipas `hallucination-pipeline`.
 * Thin wrapper → DiagramKit Block (HTML icon-chain + autoplay etalon).
 */
import HallucinationPipelineBlock from './slides/shared/HallucinationPipelineBlock';

export default function HallucinationPipelineSlide() {
  return <HallucinationPipelineBlock />;
}
